import { AppBar, Toolbar } from '@material-ui/core';
function Header() {
  return (
    <AppBar position="static">
      <Toolbar>
        Pizza portal
      </Toolbar>
    </AppBar>);
};

export default Header;